# TODO / Roadmap

## High priority
- Finalize download queue UX and reliability (progress, retries, cancel/pause).
- Add advanced filters (region, media type, cover status) to game list.
- Build bulk operations UI (multi-select, delete/move/apply actions).
- Ship statistics dashboard (charts for library, duplicates, downloads).
- Add global keyboard shortcuts for main flows.

## Backlog
- Extend metadata providers (IGDB, MobyGames) alongside RAWG.
- Improve BIN/CUE → ISO conversion (edge cases, validation).
- CDDA detection/warnings surfaced in UI.
- Windows build pipeline via CI; validate Linux support.
- Auto-update mechanism for desktop builds.

## Housekeeping
- Keep README in sync with shipped features.
- Add/adjust tests when implementing above items.
